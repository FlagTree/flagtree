#include "cnrt.h"
#include <stdint.h>

#ifdef __cplusplus
extern "C" {{
#endif

__mlu_global__ void {device_kernel_name}({signature}) {{
  return;
}}

/*
{func_docstring}
*/
cnrtRet_t {func_name[0]}(cnrtQueue_t queue, cnrtDim3_t* dim, {signature}) {{
  cnrtFunctionType_t c = (cnrtFunctionType_t){num_warps};
  cnrtDim3_t dim_with_num_warps = {{{num_warps}*dim->x, dim->y, dim->z}};
  {device_kernel_name}<<<dim_with_num_warps, c, queue>>>({arg_names});
  return cnrtSuccess;
}}

#ifdef __cplusplus
}}
#endif
